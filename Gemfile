source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}" }

# safe require
gem "digest-sha3", github: 'appliedblockchain/digest-sha3-ruby', branch: 'fix-issue-7'
gem "ethereum.rb", github: "makevoid/ethereum.rb"

gem "rake"

# in normal envs
# gem "ethereum.rb"

# locally
# gem 'ethereum.rb', path: "#{File.expand_path "~/apps/ethereum.rb"}/"
